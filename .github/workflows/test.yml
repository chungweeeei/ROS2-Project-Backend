name: Test (Container)
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DB_NAME: go_db
    steps:
      - name: Download repository
        uses: actions/checkout@v4
      - name: Install dependencies
        run: go mod download
